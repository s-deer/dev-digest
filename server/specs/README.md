# server specs

Specs describe what we intend to build here, written before or while building
it — one file per non-trivial feature or change (e.g. `specs/agent-versions.md`).
Skip a spec for routine changes; write one when the shape is worth settling
before or alongside the code.

No specs yet.
