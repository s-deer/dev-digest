# e2e docs

Deeper documentation for `e2e` — architecture notes, design decisions, or
subsystems that need more explanation than the top-level `README.md` gives —
as `docs/<topic>.md`. Executable specs live in `../specs/*.flow.json` instead
of here.

- [writing-a-flow.md](writing-a-flow.md): runner behavior between steps and flows, what a failure leaves behind, choosing locators, what the seed allows you to assert, and hermetic-stack details.
