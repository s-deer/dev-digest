# e2e specs

The `*.flow.json` files in this folder **are** the specs — each one is an
executable agent-browser flow describing a user journey we expect to keep
working (see the coverage table in `../README.md`). Prose specs for anything
not yet expressed as a flow go in `../docs/` instead.
